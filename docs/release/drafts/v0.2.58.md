# v0.2.58 — CTD 증거 유실 차단과 검증 체계 연결

## 핵심 변경
- 덤프 쓰기가 일시적으로 실패해도 사고를 잃지 않도록, 대상 프로세스가 살아 있는 동안 제한된 횟수만큼 그 자리에서 재시도합니다.
- 정상 종료(exit 0)로 분류되는 강한 결함은 빠른 필터와 지연된 프로세스 종료 어느 경로에서도 `SkyrimDiag_CleanExitEvidence_*.json`을 산출물 삭제 전에 남깁니다. JSON은 캡처 당시 결함과 실제 `dump_preserved` 상태를 기록하며, JSON 쓰기가 실패하면 덤프를 자동 보존합니다.
- CrashLogger가 우리보다 늦게 로드되는 로드오더에서도 중첩 예외 억제가 동작하도록, SKSE `kPostLoad` 시점부터 모듈 범위를 다시 조회하고 write-once 규약으로 공개합니다.
- 싱글 스텝, 스레드 이름 설정, `OutputDebugString` 예외를 강한 결함 분류에서 제외해 unsafe `CrashHookMode=2`에서도 정상 알림이 clean-exit 증거 JSON 대상이 되지 않게 했습니다. 기본 Mode 1의 치명적 예외 선택은 기존과 같습니다.

## 빌드 · 검증 체계
- `vcpkg.json`이 `CMakeLists.txt`보다 14개 릴리즈 뒤처져 있었습니다. 두 값을 맞추고, 앞으로 불일치하면 릴리즈가 실패합니다.
- 설정만 있고 실행되지 않던 clang-tidy를 Linux CI에 `WarningsAsErrors`로 연결했습니다. 검사 대상은 컴파일 데이터베이스에서 도출하므로 새 분석기 소스가 자동 포함되고, Windows 전용 소스가 게이트 밖이라는 사실은 CI 로그에 그대로 드러납니다.
- 크래시 로그 파서 퍼저를 CI에서 실제로 실행합니다. libFuzzer가 새 입력을 첫 번째 코퍼스 인자에 쓰므로 스크래치 디렉터리를 앞에 두어 검수된 시드가 오염되지 않게 했습니다.
- Windows 전용 런타임 테스트를 CI에서 실행합니다.
- `skydiag_quality_corpus_runner`가 raw signal 픽스처를 production `BuildCandidateConsensus()`로 처리해 임시 Summary를 만들고, `skydiag_quality_corpus_gate_tests`가 그 결과의 후보 순위·상태·신뢰도·점수·기권과 집계값을 검사합니다. 실사고 정확도 측정이 아니라 candidate-consensus 동작 회귀 감지입니다.
- `skydiag_ci_wiring_tests`가 위 검사들이 워크플로에서 사라지면 실패합니다.
- 버전 태그 릴리즈도 unit·ASan+UBSan·clang-tidy·parser fuzz를 직접 실행한 뒤 Windows 패키징으로 넘어갑니다.
- Windows source/XAML guard는 CRLF를 정규화하고 share-text fixture stdout은 UTF-8로 읽어, runner 로캘과 줄바꿈 때문에 테스트가 실패하지 않게 했습니다.

## 주의사항
- **2차 결함 보존 기능은 이번 릴리즈에서 제외했습니다.** 최초의 강한 결함을 이후 결함으로부터 지키는 기능을 구현했다가 릴리즈 전에 되돌렸습니다. 보존은 설계상 `crash_seq`를 움직이지 않는데 `crash_seq`가 이 프로토콜의 유일한 세대 카운터라, 헬퍼 입장에서 억제 사실이 보이지 않습니다. 버전 없는 별도 상태 플래그를 더해도 두 프로세스가 하나의 사고를 원자적으로 볼 수 없어, 검사를 추가할 때마다 다른 인터리빙이 남았습니다. 안전한 구현에는 `SharedLayout` 버전 상향과 ADR-0004 호환성 검토가 필요하며 다음 릴리즈 과제입니다. v0.2.57 대비 동시성 위험은 추가되지 않았습니다.
- 정상 종료 증거 격리는 기본적으로 JSON 메타데이터만 남깁니다. `PreserveFilteredCrashDumps=1`이면 덤프도 보존되며 JSON이 그 상태를 명시합니다.
- 검수된 실사고 코퍼스가 여전히 없으므로, 다른 크래시 로거 대비 적중률을 수치로 주장하지 않습니다.
- 플러그인, Helper, 분석기와 WinUI가 함께 바뀌므로 기존 파일 일부만 덮지 말고 zip 전체를 업데이트해 주세요.

## 검증
- Windows native build: 성공
- Windows test suite: `64/64` 통과
- Windows WinUI self-contained publish: 성공
- Ubuntu Linux build: 성공
- Linux test suite: `60/60` 통과
- clang-tidy(`WarningsAsErrors`): clean
- Packaging: `dist/Tullius_ctd_loger_v0.2.58.zip` 생성 성공 (`--no-pdb`, `87,783,466` bytes, 523 entries, PDB 0개)
- Release gate: `OK` (핵심 PE/Windows App SDK x64, 현재 빌드 해시 일치, 버전 소스 일치)
- 실사고 품질 코퍼스: `SKIPPED (not measured)` — 코퍼스 미제공
- SHA-256: `ED3C69AA09B2CD05707511B3C2194D80A69E41ACB65ABEC12656C9A5F4F6A501`
